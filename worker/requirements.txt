fastapi>=0.104.0
uvicorn[standard]>=0.24.0
# Used by bundled agents (e.g. agent1) when /venvs/<id> is missing or not ready yet
beautifulsoup4>=4.12
# Fallback when code_files omit requirements or stale venv cache had no pip install yet
openai>=1.0
httpx>=0.27.0
